            <nav class="header-menu">
                <ul class="header-button">
                    <li><a href="{{ path('app_home') }}"><button>Accueil</button></a></li>
                    <!--<li><a href="{{ path('app_account') }}"><button>Covoiturages</button></a></li>--!>
                    <!--<li><a href="#contact-menu"><button>Contact</button></a></li>--!>
                    {% if app.user %}
                        <li><a href="{{ path('app_account') }}"><button>Mon Compte</button></a></li>
                        <li><a href="{{ path('app_logout') }}"><button>Se déconnecter</button></a></li>
                    {% else %}
                        <li><a href="{{ path('app_login') }}"><button>Connexion</button></a></li>
                    {% endif %}
                    <li><a href="{{ path('user_history') }}"><button>Historique</button></a></li>

                    {% if is_granted('ROLE_EMPLOYE') %}
                        <li><a href="{{ path('app_review') }}"><button>Employé</button></a></li>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li><a href="{{ path('admin_dashboard') }}"><button>Administrateur</button></a></li>
                    {% endif %}
                    {% if is_granted('ROLE_CHAUFFEUR') %}
                        <li><a href="{{ path('trip_new') }}">Créer un trajet</a></li>
                    {% endif %}

                </ul>
            </nav>
