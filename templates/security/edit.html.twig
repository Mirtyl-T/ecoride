{% extends 'base.html.twig' %}

{% block title %}Modifier le véhicule{% endblock %}

{% block body %}
    <h1>Modifier le véhicule</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-warning mt-2">Modifier</button>
        <a href="{{ path('vehicule_index') }}" class="btn btn-secondary mt-2">Retour</a>
    {{ form_end(form) }}
{% endblock %}
